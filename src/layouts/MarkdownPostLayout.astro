---
import BaseLayout from "./BaseLayout.astro";

const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle = {frontmatter.title}>
    <article>
        <p>Published: {frontmatter.published.toString().slice(0,10)}</p>
        <!-- <div>tags</div> -->
        <slot />   
    </article>
</BaseLayout>